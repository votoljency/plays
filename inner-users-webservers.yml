    - include_tasks: inner-users-database-webservers-sshkey.yml
      loop: "{{users}}"
      when: '"{{item.uid}}" is match("1") and ansible_hostname in outer_item'

    - name: msg groups inner users webservers
      debug:    
        msg: "{{ outer_item }}"
