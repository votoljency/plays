    - include_tasks: inner-users-database-webserver-sshkey.yml
      loop: "{{users}}"
      when: '"{{item.uid}}" is match("2") and ansible_hostname in outer_item'
#     with_items: "{{groups['webservers']}}"  
#     when: ansible_hostname !=  item  or ansible_hostname ==  item
