    - name: new name
      include_tasks: inner-users-database-webservers-sshkey.yml
      loop: "{{users}}"
      when: '"{{item.uid}}" is match("2") and ansible_hostname in outer_item'

    - name: msg groups
      debug:    
        msg: "{{ outer_item }}"