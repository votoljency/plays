---
- name: list facts
  become: yes
  remote_user: automation
  hosts: proxy
  gather_facts: yes
  tasks:
    - name: facts
      debug:
        msg: "{{ [client1.ipa.vm[ansible_version]] }}"

    - name: facts variation
      debug:
        msg: "{{ [ansible_version] }}"        