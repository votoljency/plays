all:
  
  children:
    proxy:
      hosts:
        3rd.ipa.vm:
      vars:

    webservers:
      hosts:
        2nd.ipa.vm:
      vars:

    database:
      hosts:
        client4.ipa.vm:
      vars:
        
    clockworknet:
      hosts:
        client3.ipa.vm:

    ipaserver:      
      hosts:
        1st.ipa.vm:
        #  ansible_connection: local
      vars:
      #  ipaadmin_password: 123456789
      #  ipadm_password: 123456789
        ipaserver_realm: IPA.VM
        ipaserver_domain: ipa.vm
        ipaserver_setup_dns: yes
        ipaserver_auto_forwarders: yes


    ipaclients:
      hosts:
        3rd.ipa.vm:
      vars:
        ipaadmin_principal: admin
        ipaclient_domain: ipa.vm
        ipaclient_servers: 1st.ipa.vm

    test:
      hosts:
        client3.ipa.vm: