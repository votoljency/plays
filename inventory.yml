all:
  
  children:
    proxy:
      hosts:
        client1.ipa.vm:
      vars:

    webservers:
      hosts:
        2nd.ipa.vm:
      vars:

    database:
      hosts:
        client4.ipa.vm:
      vars:
        
    clockworknet:
      hosts:
        client3.ipa.vm:

    ipaserver:      
      hosts:
        1st.ipa.vm:
        #  ansible_connection: local
      vars:
        ipadm_password: 123456789
        ipaadmin_password: 123456789
        ipaserver_realm: IPA.VM
        ipaserver_domain: ipa.vm

    test:
      hosts:
        client3.ipa.vm: