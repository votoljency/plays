all:
  
  children:
    proxy:
      hosts:
        client1.ipa.vm:
      vars:

    webservers:
      hosts:
      #  client2.ipa.vm:
        client3.ipa.vm:
      vars:

    database:
      hosts:
        client4.ipa.vm:
      vars:
        
    clockworknet:
      hosts:
        client3.ipa.vm:

    ipaserver:      
      hosts:
        master.ipa.vm:
        #  ansible_connection: local
    test:
      hosts:
        client3.ipa.vm: